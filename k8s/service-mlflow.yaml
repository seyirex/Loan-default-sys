apiVersion: v1
kind: Service
metadata:
  name: mlflow-service
  namespace: loan-default-prediction
  labels:
    app: mlflow-server
spec:
  type: ClusterIP  # Internal only, expose via Ingress
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: mlflow-server
